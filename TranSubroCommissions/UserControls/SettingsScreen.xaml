<local:InjectableUserControl x:Class="TranSubroCommissions.SettingsScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TranSubroCommissions"
             xmlns:m="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Padding="15">
            <StackPanel>
                <TextBlock Margin="0 0 0 30" FontSize="24">Settings</TextBlock>
         
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <TextBox Margin="0 0 15 0" Grid.Column="0" Name="fileLocation" m:HintAssist.Hint="Company File Location" m:HintAssist.IsFloating="true" />
                    <Button Name="directorySelect" m:ShadowAssist.ShadowDepth="Depth0" Grid.Column="1" Click="directorySelect_Click" Content="Browse"></Button>
                </Grid> 
            </StackPanel>
        </Border>
    </Grid>
</local:InjectableUserControl>
