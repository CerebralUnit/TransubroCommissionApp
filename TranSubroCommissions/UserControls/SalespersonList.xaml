<local:InjectableUserControl x:Class="TranSubroCommissions.SalespersonList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TranSubroCommissions"
                              xmlns:m="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"  Background="#FFFFFF"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" Name="SalespersonsList"  CanUserAddRows="False" Background="#FFF">
                <DataGrid.Columns> 
                    <DataGridTextColumn Header="Id"  Binding="{Binding Path=Id}" Width="211" />
                    <DataGridTextColumn Header="Salesperson"  Binding="{Binding  Path=Name }" Width="*" /> 
                </DataGrid.Columns>
            </DataGrid> 
        </StackPanel>
    </Grid>
</local:InjectableUserControl>
